package com.supplierservice.supplierservice.models;

public enum AuditAction {
    CREATED, UPDATED, STATUS_CHANGED, RECEIVED, CANCELLED,
    ITEM_ADDED, ITEM_UPDATED, ITEM_REMOVED,
    NOTE_ADDED, ATTACHMENT_ADDED
}
