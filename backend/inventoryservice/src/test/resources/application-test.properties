# Test Configuration for Inventory Service
spring.application.name=inventoryservice-test
server.port=0

# Test Database Configuration (H2 for unit tests)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration for H2
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Kafka Configuration (Test)
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=inventoryservice-test-group
spring.kafka.consumer.auto-offset-reset=earliest

# Logging Configuration
logging.level.org.springframework=WARN
logging.level.com.InventoryMangementSystem.inventoryservice=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Test Profile - automatically activated by Spring Boot Test